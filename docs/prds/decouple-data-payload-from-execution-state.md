data payloads are instantiated in events and in the execution model,
leads to lots of duplication of data in memory and in storage.

Could decouple data payloads from execution state with separate storage and grab data as needed.